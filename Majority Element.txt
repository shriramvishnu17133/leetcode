class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        s={}
        lens=len(nums)/2
        for i in nums :
            s[i]=s.get(i,0)+1
            if s[i]>lens:
                return i